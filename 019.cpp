#include <iostream>

using namespace std;

int main() {
    int sundays = 0;
    for(every year) {
        // Catch weird cases
        for(every month) {
            // Catch weird cases
            for(every week) {
                // Catch weird cases
                sundays++;
            }
        }
    }
}
