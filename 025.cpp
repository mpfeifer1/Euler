#include <iostream>

using namespace std;

int main() {
    desiredLength = 1000;
    int array1[desiredLength];
    int array2[desiredLength];

    array1[0] = 1;
    array2[0] = 1;

    while(current < search) {
        past += current;
        swap(current, past);
        cout << current << endl;;
    }
}
